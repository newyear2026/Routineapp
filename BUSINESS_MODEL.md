# BUSINESS_MODEL.md

이 문서는 `Routine Timer`의 수익 모델(BM) 방향을 정의한다.
현재 앱은 **로컬 전용 · 계정 없음 · 서버 없음**(`PROJECT_RULES.md` 1·5장) 구조이므로,
BM도 이 제약을 깨지 않는 순서로 단계적으로 붙인다.

---

## 1. 결론 (권장안)

**"평생 이용권 우선 → 구독 확장" 형태의 프리미엄(freemium) IAP**를 축으로 하되,
**톤을 훼손하지 않는 형식의 광고를 IAP보다 먼저 붙인다.**

이유:
- 이 앱의 차별점은 기능량이 아니라 **원형 24시간 시간표 · 홈 위젯 · 감성 테마**다.
  이런 앱에서 지갑을 여는 동기는 "기능 해금"보다 **꾸미기 + 내 기록을 지키는 것**이다.
- 서버 비용이 0에 가까우므로 **평생 이용권(비소모성 IAP)의 마진이 매우 좋다**.
  계정·서버 없이도 스토어 복원(restore)만으로 구현 가능해 현재 아키텍처와 충돌하지 않는다.
- 다만 **IAP를 붙이기 전에 알아야 할 것이 두 가지** 있는데, 광고가 그 둘을 먼저 준다.

광고를 먼저 넣는 근거:

1. **계측이 광고와 같은 비용을 쓴다.** `INTERNET` 권한 추가, 데이터 안전성
   재신고, 개인정보처리방침 개정, 재심사 — 이 비용은 광고를 넣든 분석 도구를
   넣든 똑같다. 두 번 치를 이유가 없어 같은 릴리스에 태운다. 이로써 5장 Phase 0이
   유료화의 전제로 못박은 D1·D7·D30 리텐션 계측이 해결된다.
2. **보상형 광고 이용률이 지불 의사의 선행 지표다.** 몇 %가 테마 하나를 위해
   30초를 내는지 알면, 3장의 평생 이용권 가격을 추측이 아니라 근거로 정할 수 있다.
   8장의 "전환율 2~5%"를 검증 없이 믿고 가는 것보다 낫다.

단, 형식을 가린다. **배너·전면·앱 오프닝은 여전히 넣지 않는다** (7장).
자리와 상한은 `flutter_app/docs/AD_PLACEMENT.md`가 정한다.

---

## 2. Free / Pro 경계

핵심 원칙: **"하루를 보는 경험"은 절대 유료로 막지 않는다.**
원형 시간표, 현재/다음 루틴, 완료·나중에·스킵, 기본 위젯은 무료다.
막는 순간 리텐션이 죽고, 리텐션이 없으면 어떤 BM도 성립하지 않는다.

| 영역 | Free | Pro |
|------|------|-----|
| 원형 시간표 · 현재/다음 루틴 · 액션 | 전체 | 전체 |
| 루틴 개수 | 10개 | 무제한 |
| 테마 프리셋 | 2종 | 4종 + 시즌 테마 |
| 루틴 색상 · 아이콘 | 기본 팔레트 | 확장 팔레트 · 아이콘 팩 |
| 홈 위젯 | 미디엄 1종 기본 스킨 | 사이즈·스킨 선택 |
| 기록 | 최근 7일 | 전체 기록 · 주/월 리포트 · 스트릭 |
| 루틴 세트 | 1개 | 다중 세트(평일/주말/시험기간 전환) |
| 백업 · 복원 | 수동 내보내기 | 자동 백업(추후 클라우드 동기화) |
| 알림 | 기본 | 스누즈 간격·사운드 커스텀 |

의도적으로 이렇게 나눈 이유:
- **루틴 개수 제한은 약하게 잡는다.** 24시간 시간표에서 5개는 화면이 비어 보이고 앱의 인상을 망친다.
  10개는 "충분히 써 보고, 진지해지면 걸리는" 선이다.
- **루틴 세트 전환**이 가장 정직한 유료 트리거다. 평일/주말을 분리하고 싶은 사용자는 이미 앱을 습관화한 사용자다.
- **기록·리포트**는 시간이 지날수록 가치가 커지므로, D14~D30 시점의 전환을 만든다.

---

## 3. 가격 (한국 기준 초안)

| 상품 | 가격 | 비고 |
|------|------|------|
| 평생 이용권 | 29,000 ~ 39,000원 | 1순위 상품 |
| 연간 구독 | 14,000 ~ 19,000원 | 월 대비 60~70% 할인 표기 |
| 월간 구독 | 2,900 ~ 3,900원 | 앵커 역할 |
| 시즌 테마 팩 | 3,000 ~ 5,000원 | Pro는 포함 |

- 첫 배포는 **평생 이용권 단일 상품**으로 시작해 가격 탄력성을 본다.
- 구독은 **시즌 테마가 정기적으로 나오기 시작한 뒤**에 붙인다.
  갱신되는 가치가 없는 구독은 환불·악평의 원인이 된다.

---

## 4. 페이월 위치

- 온보딩 직후에는 **띄우지 않는다.** 첫 세션 페이월은 이 카테고리에서 설치 이탈만 키운다.
- 노출 지점:
  1. 첫 "루틴 전부 완료" 축하 화면 (성취 직후)
  2. Pro 테마 · 위젯 스킨 탭 (문맥 페이월)
  3. 11번째 루틴 추가 시도
  4. 7일 이전 기록 조회 시도
  5. 설정 > Pro 진입점 (상시)
- 페이월은 기능 목록이 아니라 **테마·위젯 미리보기 이미지** 중심으로 만든다.

---

## 5. 단계별 로드맵

### Phase 0 — 계측 + 광고 (유료화 전, 필수)
- D1 / D7 / D30 리텐션, 루틴 생성 수, 위젯 설치율, 액션(완료/스누즈/스킵) 비율 수집.
- **계측과 광고를 같은 릴리스에 태운다.** `INTERNET` 권한·데이터 안전성 재신고·
  개인정보처리방침 개정·재심사 비용이 둘에 공통이라, 나눠 내면 두 번 낸다.
- 광고는 **네이티브 1자리(홈) + 보상형 1자리(설정)**로 시작한다.
  자리·상한·단계는 `flutter_app/docs/AD_PLACEMENT.md`가 정한다.
- **D30 리텐션 20% 미만이면 유료화하지 않는다.** 먼저 리텐션을 고친다.
  광고를 켠 뒤 이 수치가 흔들리는지가 첫 관문이다.
- 데이터 소실 방어를 위해 **내보내기/가져오기(JSON)를 유료화보다 먼저** 넣는다.
  로컬 전용 앱에서 기기 변경 시 기록이 날아가면, 유료 사용자에게는 환불 사유가 된다.

### Phase 1 — 평생 이용권 (계정 없이 가능)
- `in_app_purchase` 비소모성 상품 1종 + 복원.
- 엔타이틀먼트는 로컬 저장(`SettingsRepository`와 같은 계층).
- 서버 영수증 검증 없음 — 이 규모에서는 감수 가능한 위험으로 본다.
- **가격은 Phase 0의 보상형 광고 이용률을 근거로 정한다.** 테마 하나에 30초를
  내는 비율이 3장 가격표의 검증 자료다.
- 구매 즉시 네이티브 광고는 사라진다. 보상형은 시즌 테마 체험용으로 남긴다.

### Phase 2 — 구독 · 테마 팩
- 크로스 플랫폼 영수증/엔타이틀먼트 관리를 위해 RevenueCat 도입 검토(서버 없이 운영 가능).
- 시즌 테마를 분기 1회 리듬으로 공급.

### Phase 3 — 계정 · 클라우드 동기화
- 기기 간 동기화 수요가 확인된 뒤에만 계정을 도입한다.
- 이 시점에 구독의 정당성이 생기고, 구독 비중을 늘린다.

---

## 6. 구현 시 지켜야 할 구조 규칙

`PROJECT_RULES.md` 4·5장을 그대로 따른다.

- 결제 상태 저장은 **repository 계층**에만 둔다 (`PurchaseRepository` / 엔타이틀먼트 저장).
- 화면에서 `isPro`를 직접 분기하지 않는다. **`FeatureGateService` 한 곳**에서
  `gate.can(Feature.themePack)` 형태로 판정하고, 화면은 그 결과만 쓴다.
  게이팅 조건이 화면에 흩어지면 가격 정책 변경이 불가능해진다.
- 테마는 `AppThemePreset`에 `isPro` 플래그를 추가해 데이터로 구분한다.
- 잠긴 기능은 숨기지 않고 **잠금 상태로 보여준다.** (`PROJECT_RULES.md` 9장:
  비활성 액션은 이유를 함께 설명한다.)
- 백업/복원은 모델에 이미 `toJson` / `fromJson`이 있으므로 repository 계층에서 확장한다.
- **광고 노출 판정도 같은 규칙을 따른다.** 화면은 조건을 직접 들지 않고
  `AdSlotPolicy` 한 곳의 판정 결과만 쓴다. 상한과 자리별 조건이 화면에 흩어지면
  광고 정책을 바꿀 수 없게 된다. `FeatureGateService`가 생기면 이 판정기를
  그 안으로 흡수한다.

---

## 7. 수익 형식별 판단

| 안 | 판단 |
|----|------|
| 네이티브 광고 | **채택.** 카드 목록에 같은 모서리로 섞이고 스타일을 앱이 지정한다. 톤을 지킬 수 있는 유일한 형식 |
| 보상형 광고 | **채택.** 사용자가 먼저 누르는 교환이라 리텐션 위험이 0이고, 지불 의사 측정을 겸한다 |
| 배너 광고 | 320×50 고정 사각형이 라운드 카드 문법 밖이다. 체류 3초 앱에서 수익 기여는 작은 반면 톤 비용은 그대로 낸다. 채택하지 않음 |
| 전면 · 보상형 전면 광고 | 유일하게 사용자를 가로막는다. D30 리텐션 20%를 넘긴 뒤 재검토 |
| 앱 오프닝 광고 | "지금 뭐 할 차례지"를 확인하는 3초를 통째로 소비한다. 영구 제외 |
| 소셜 · 챌린지 · 커뮤니티 | 운영 비용 대비 회수 불확실. MVP 범위 밖 |
| B2B(학원·코치 템플릿 배포) | 초기 단계에서는 분산. 개인 사용자 리텐션 확보 후 재검토 |
| AI 루틴 추천 유료화 | 추론 비용이 붙어 평생 이용권과 상충. 도입 시 별도 상품으로 분리 |

---

## 8. 성공 기준

- 무료 → 유료 전환율 **2~5%** (문맥 페이월 기준)
- 페이월 노출 대비 구매 **3% 이상**
- 유료 사용자 D90 리텐션이 무료 대비 **2배 이상**
- 환불율 **2% 미만**

이 수치가 나오지 않으면 가격이 아니라 **Free/Pro 경계**를 먼저 고친다.
